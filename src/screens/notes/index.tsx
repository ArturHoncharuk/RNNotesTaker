import { SafeViewContainer, Typography } from '~/shared/ui';

export function NotesScreen() {
  return (
    <SafeViewContainer>
      <Typography cn="font-bold">Notes</Typography>
    </SafeViewContainer>
  );
}
